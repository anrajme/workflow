---
name: "update_ticket"
action_ref: "workflow.update_ticket_workflow"
description: "Update the zendesk ticket"
formats:
    - display: "update ticket {{comment}}"
      representation:
          - "ticket update {{comment}}"
ack:
    format: "Hold tight while I run command: on hosts"
result:
    format: |

        Details are as follows:
        ---> {{execution.result.output.message}}