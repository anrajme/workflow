---
name: cloud_check_disk
runner_type: local-shell-script
description: Check disk utilization of remote cloud.
enabled: true
entry_point: cloud_check_disk.sh
parameters:
  envname:
    type: "string"
    required: true
    position: 1
    default: "{{action_context.source_channel}}"
#notify:
# on-complete:
#   message: "{{action_context}}"
#   routes:
#     - "slack"
#   data:
#     channel: "{{action_context.message.source_channel}}"